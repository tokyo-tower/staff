<%
let routeName = '';
if (req.hasOwnProperty('route')) {
    routeName = req.route.path;
}
%>
<header>
    <div class="container">

        <a href="javascript:void(0);" class="header-logo"><img src="/images/common/logo.svg" alt="TTTS"></a>

        <div class="header-menu">
            <% if (routeName.indexOf('/checkin/performance/') > -1 && routeName.indexOf('/confirm') > -1) { %>
            <a class="navbar-brand" href="/checkin/performances">戻る</a>
            <% } %>
        </div>
    </div>
</header>
